# Compacts archives
*.zip
**/*.zip # Ignora em subdiretorios tambem

# Bibliography
*.pdf
**/*.pdf # Ignora em subdiretorios tambem

# Audios
*.mp4
**/*.m4a # Ignora arquivos .m4a em qualquer subdiretorio (como em Aulas/)

# KiCAD specific files
# Arquivos de cache e backup
./hardware/*.bak
./hardware/*.fbl
./hardware/fp-info-cache
./hardware/eeg-backups/
# Arquivos de cache de componentes
./hardware/*.cmp
./hardware/*.dcm
./hardware/*.kicad_mod
./hardware/*.lib
# Regras mais abrangentes para KiCAD em qualquer subdiretorio que tenha estrutura similar
**/*.bak
**/*.fbl
**/fp-info-cache
**/eeg-backups/
**/*.cmp
**/*.dcm
**/*.kicad_mod
**/*.lib
**/bom.csv
**/*.log
# Ignora o conteudo de pastas .pretty/ em qualquer lugar (se forem apenas caches)
**/*.pretty/
# Arquivos gerados que podem variar
**/fp-lib-table
**/sym-lib-table

# STM32CubeIDE caches
# General IDE metadata and logs
./firmware/.metadata/
./firmware/.settings/
./firmware/*.launch
./firmware/*.log
# Build output directories
./firmware/Release/
./firmware/Debug/
./firmware/*.elf
./firmware/*.hex
./firmware/*.bin
# Ignora arquivos gerados automaticamente por ferramentas de analise de codigo
./firmware/**/*.cyclo
./firmware/**/*.list
./firmware/**/*.map
./firmware/**/*.size
./firmware/**/*.su
./firmware/**/*.o
# Regras mais abrangentes para STM32CubeIDE em qualquer subdiretorio que tenha estrutura similar
**/.metadata/
**/.settings/
**/*.launch
**/*.log
**/Release/
**/Debug/
**/*.elf
**/*.hex
**/*.bin
**/*.cyclo
**/*.list
**/*.map
**/*.size
**/*.su
**/*.o

# Prerequisites
*.d

# Object files
*.o
*.ko
*.obj

# Linker output
*.ilk
*.map
*.exp

# Precompiled Headers
*.gch
*.pch

# Libraries
*.lib
*.a
*.la
*.lo

# Shared objects (inc. Windows DLLs)
*.dll
*.so
*.so.*
*.dylib

# Executables
*.exe
*.out
*.app
*.i*86
*.x86_64

# Debug files
*.dSYM/
*.su
*.idb
*.pdb

# Kernel Module Compile Results
*.mod*
*.cmd
.tmp_versions/
modules.order
Module.symvers
Mkfile.old
dkms.conf

# Arquivos e Pastas Pessoais
# (O 'Documents/' nao eh um bom padrao se ele contem arquivos que voce quer versionar,
# como os PDFs que voce inicialmente tentou ignorar e depois removeu.
# Melhor ser especifico ou nao o ignorar se contiver conteudo misto.)
Aulas/
EEGV5/
EEGV6/
Literatura/
Documents/ # ATENCAO: Se voce tem subpastas em Documents que quer versionar, remova esta linha.
           # Caso contrario, ela ignorara TUDO dentro de Documents/.
